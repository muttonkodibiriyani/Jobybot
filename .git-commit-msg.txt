Enterprise rebrand: jobybots.com, FAQ + refund, security hardening

Brand
- HD logo (1 MB PNG) + slogan "Your AI Job Hunter. 24/7. On Your Laptop."
- Header / Footer / Hero / OG image / favicon all reference /jobybots-logo.png.
- Compact inline SVG mark for header; full HD raster on hero / OG.
- Domain: jobybots.com is the new canonical URL throughout.

Payment
- Real PhonePe QR (DARAPU THARAKESWARA REDDY) shipped at /public/phonepe-qr.png.
- /buy-india now renders that QR with payee name + step-by-step UPI instructions
  and a 7-day refund highlight.
- lib/config.ts adds SUPPORT (email, phone, hours, refund window) and PAYMENT
  constants; .env.example documents every env var.

FAQ
- New /faq page covering payments, refunds (7-day), product, security, support.
- Big email + phone contact cards. Accordion sections. Final "real humans"
  CTA with email + phone buttons.

Refund (7-day money-back guarantee)
- /refund page + RefundForm component.
- /api/refund: enforces 7-day window server-side, verifies order email match,
  rate-limited 4/hr/IP, suspicious activity alerts, sends customer ack +
  admin notify.
- /api/admin/refunds: approve / reject + auto-email customer.
- /admin dashboard: refunds tab + recently-completed tab + security events.
- /api/cron/notify-pending now includes pending refunds in the 30-min digest.
- docs/REFUND_POLICY.md is the customer-facing policy.

Security
- middleware.ts adds HSTS preload, CSP, X-Frame-Options DENY, COOP / CORP,
  Permissions-Policy, Referrer-Policy strict-origin-when-cross-origin.
- lib/security.ts: clientIp, in-memory rate limit, trackSuspicious helper
  that auto-emails the owner when an event threshold trips inside 10 min.
- Per-route rate limits: /api/india-order 6/hr, /api/refund 4/hr,
  /api/admin/login 5 / 10 min.
- security_events table in SQLite + recentSecurityEvents on /admin.
- countSecurityEventsSince used by trackSuspicious to email at thresholds:
  admin_login_failed >= 3, admin_login_rate_limit >= 1, others >= 5.
- docs/DOMAIN_SECURITY.md: HSTS preload, CAA records, Vercel Firewall,
  optional SPF/DKIM/DMARC for custom sender, incident-response runbook.

Enterprise positioning
- ComparisonTable: JobyBots vs Sonara vs LazyApply vs AIApply across
  pricing, where it runs, account access risk, deliverability, GDPR mode,
  refund policy, markets covered, support channels.
- Home page rewrite: HD logo hero, trust badges, comparison band, six
  security cards, Real Humans support band, dual CTAs (UPI + card).

SEO / Google ranking
- Layout metadata: full title template, keyword set targeting "Sonara
  alternative", "LazyApply alternative", "200 applications per day",
  Indian market terms.
- Both SoftwareApplication + Organization JSON-LD (with slogan + contact).
- OG image set to /jobybots-logo.png (1200x630).
- sitemap.xml + robots.txt updated with /faq, /refund.

Documentation
- docs/DEPLOY_VERCEL.md: step-by-step Vercel deploy, env vars, GoDaddy DNS
  records for jobybots.com + www, Vercel KV setup, cron verification,
  Google Search Console submission, common gotchas.
- docs/DOMAIN_SECURITY.md: end-to-end hardening checklist.
- docs/REFUND_POLICY.md: customer-facing policy.
- README updated with new website section, support contact, doc links.
