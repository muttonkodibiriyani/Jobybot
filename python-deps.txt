requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
# httpx is used by both the scrapers and the AI provider clients
# (Gemini / Groq). Stay on a recent version with HTTP/2 support.
httpx[http2]==0.27.2
pdfplumber==0.11.4
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1
apscheduler==3.10.4
dnspython==2.7.0
loguru==0.7.2
click==8.1.7
jinja2==3.1.4
rich==13.9.4
tenacity==9.0.0
# ── Optional: LinkedIn Easy Apply ─────────────────────────────────
# Playwright is only required if you enable ENABLE_EASY_APPLY=true.
# After install, also run:  python -m playwright install chromium
# (~150MB Chromium download — happens once.)
playwright==1.50.0
# ── Optional: AI fallback for unknown Easy Apply questions ───────
# Used only if a GEMINI_API_KEY is set. Free tier is generous.
google-generativeai==0.8.3
