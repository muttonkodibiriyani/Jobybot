{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd website && npm install && npm run build",
  "outputDirectory": "website/.next",
  "installCommand": "echo \"Root install skipped — runs inside website/ via buildCommand\"",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/notify-pending",
      "schedule": "*/30 * * * *"
    }
  ]
}
