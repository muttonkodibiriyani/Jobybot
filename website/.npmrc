# If better-sqlite3 (optional) fails to install on Vercel's serverless
# build env, don't fail the whole install. We fall back to in-memory /
# Vercel KV at runtime.
optional=true
fund=false
audit=false
